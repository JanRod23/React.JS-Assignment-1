import React from 'react';

const userOutput = (props) => {
    return (
        <div>
          <p>{props.parag[0]}</p>
          <p>{props.parag[1]}</p>
        </div>
    )
}

export default userOutput;